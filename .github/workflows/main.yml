name: CI

on: push

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo "Hello, world!"

      - name: move to home folder
        run: cd /home/animyrju

      - name: list home folder
        run: ls 